1)url:-localhost:3000/employee?remove=salary

Here we pass query string alonbg with url.
app will take those query string as input and removes salary property from employeeDetails for display;
we will get employee object without salary as a result

2)url:-localhost:3000/employee?product=dev

Here we pass query string alonbg with url.
app will take those query string as input and checks if project key is available or not 
And then give us the employee details based on project we have sprcified

In above both case we send get request from post man throug url only.

1)url:-localhost:3000/employee/ctc

Here we call url.
we will get the total salaries of employees. 
